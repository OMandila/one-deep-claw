# Notebooks

Use this directory for small, exploratory experiments such as testing prompts or inspecting public market-data responses. Keep production application code in `app/` and do not put API keys in notebooks.